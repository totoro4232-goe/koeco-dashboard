# 대한민국 경제 나침반 — 설계 문서

## 1. 목적과 범위

이 애플리케이션은 **한국 경제의 핵심 지표**(기준금리, 원/달러 환율, 소비자물가지수, CMA 잔고)를 한 화면에서 확인하고, 상세 페이지에서 추이와 부가 정보를 탐색할 수 있도록 하는 **읽기 전용 대시보드**입니다.

- **대상 사용자**: 일반 이용자, 경제 지표를 빠르게 참고하려는 사용자
- **비목표**: 사용자 계정, 데이터 수정, 실시간 트레이딩, 백엔드 영구 저장소

---

## 2. 기술 스택

| 영역 | 선택 | 비고 |
|------|------|------|
| 프레임워크 | Next.js 14 (App Router) | 서버 컴포넌트에서 공공 API 호출 |
| UI | React 18, Tailwind CSS | 유틸리티 우선 스타일링 |
| 차트 | Recharts | 라인·복합(막대+라인) 차트 |
| 언어 | TypeScript | 공통 타입은 `src/types`에 정의 |

---

## 3. 시스템 아키텍처

### 3.1 요청 흐름

1. 사용자가 `/` 또는 `/detail/*`에 접속합니다.
2. **서버 컴포넌트**가 `searchParams`(예: `period`)를 읽어 조회 기간을 결정합니다.
3. `src/lib/api/*` 모듈이 외부 REST API를 호출하고, 응답을 **도메인 친화적 구조**(시계열 포인트 등)로 변환합니다.
4. 페이지가 요약 카드·차트·테이블 등을 렌더링합니다.

### 3.2 디렉터리 역할

- **`src/app`**: 라우트, 페이지 단위 데이터 조합, 메타데이터
- **`src/components`**: 재사용 UI(대시보드 카드, 차트 래퍼, 레이아웃 헤더/푸터 등)
- **`src/lib/api`**: ECOS·KOSIS·금융위(공공데이터포털) 클라이언트
- **`src/lib/utils`**: 기간 계산, 숫자·날짜 포맷 등 순수 함수
- **`src/types`**: 공유 타입 및 외부 API row 형태

### 3.3 데이터 소스와 책임 분리

| 모듈 | 출처 | 지표 |
|------|------|------|
| `ecos.ts` | 한국은행 ECOS Open API | 기준금리, 환율(통화 코드 매핑) |
| `kosis.ts` | KOSIS Open API (+ 필요 시 ECOS 보조) | CPI |
| `fsc.ts` | 공공데이터포털 금융투자협회 CMA API | CMA 일별 잔고 |

각 모듈은 **URL 구성, 오류 메시지, 재시도·폴백 정책**을 캡슐화하고, 페이지는 `Promise.allSettled`로 병렬 호출 후 개별 성공/실패를 UI에 반영합니다.

### 3.4 캐시와 갱신

외부 `fetch`에는 `next: { revalidate: 3600 }`을 사용해 **ISR 스타일의 1시간 재검증**을 기본으로 둡니다. 공공 API 부하와 최신성 사이의 균형을 맞추기 위한 설계입니다.

---

## 4. UI·UX 설계

### 4.1 톤앤매너

- **브랜드 카피**: “대한민국 경제 나침반” — 한눈에 방향을 잡는다는 은유
- **배경**: 연한 블루 톤(`bg-primary`) + 은은한 그리드 패턴(레이아웃 루트)
- **타이포**: 본문 `Noto Sans KR`, 수치·라벨 보조 `DM Mono`

### 4.2 디자인 토큰 (Tailwind 확장)

- **배경**: `bg-primary`, `bg-secondary`, `bg-tertiary`
- **테두리**: `border`, `border-hover`
- **의미 색**: `accent`(포인트), `up`/`down`(증감), `amber`(차트 보조선 등)

### 4.3 정보 구조

- **홈 (`/`)**: 기간 필터 → 요약 카드 그리드 → 추이 차트 → 하단 요약 배너
- **상세 (`/detail/*`)**: 지표별 심화 화면(통화/표 등은 각 페이지에서 정의)

클라이언트 상호작용이 필요한 부분(예: 헤더의 현재 시각)만 `'use client'`로 제한하고, 데이터 집약 구간은 서버에서 처리합니다.

---

## 5. 도메인 모델

- **`Period`**: `6m` | `1y` | `3y` — URL 쿼리 `period`와 연동
- **`DataPoint`**: `{ date, value }` — ECOS·CPI 시계열 공통 형태
- **`CmaDataPoint`**: `{ date, balance, change }` — CMA 전용

상세 타입은 `src/types/index.ts`를 단일 출처로 유지합니다.

---

## 6. 복원력·오류 처리

- **병렬 호출**: 한 소스가 실패해도 나머지 카드/차트는 표시 (`allSettled`)
- **ECOS**: 짧은 기간 조회 결과가 비면 **장기 폴백 URL**로 재조회
- **KOSIS**: 엔드포인트 후보를 순회하며 첫 성공 응답을 사용
- **CMA**: 엔드포인트·기준일 후보를 조합해 재시도; 응답 지연 시 UI에 경고 톤 표시

---

## 7. 환경 변수

| 변수 | 용도 |
|------|------|
| `ECOS_API_KEY` | 한국은행 ECOS |
| `KOSIS_API_KEY` | KOSIS 및 공공데이터포털(금융위 CMA) 서비스키 |

배포 환경에서는 키를 서버 환경에만 두고, 클라이언트 번들에 노출되지 않도록 합니다.

---

## 8. 확장 시 고려사항

- **지표 추가**: `src/lib/api`에 클라이언트 추가 → 타입 확장 → 홈/상세 그리드에 카드·차트 배치
- **통화 확장**: `Currency` 타입과 `FX_CODE` 매핑을 함께 갱신
- **접근성**: 차트 대체 텍스트, 키보드 포커스 순서, 색만으로 의미를 전달하지 않기
- **관측 가능성**: 외부 API 실패율 로깅(서버 측) 또는 Edge에서의 타임아웃 정책

---

## 9. 용어 정리

- **ECOS**: 한국은행 경제통계시스템 Open API
- **KOSIS**: 국가통계포털(KOSIS) Open API
- **CMA**: 종합자산관리계좌 — 본 서비스에서는 금융투자협회 공개 통계의 일별 잔고 추이를 사용

이 문서는 코드베이스의 현재 구조를 기준으로 작성되었으며, 라우트나 API 정책이 바뀔 때 함께 갱신하는 것을 권장합니다.
